/** Right-pane placeholder shown at "/sessions" before any row is selected. */
export function SessionEmpty() {
  return <div className="session-empty">Select a session to view its transcript summary.</div>;
}
